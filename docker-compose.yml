services:
  scim-demo:
    build: .
    ports:
      - "3000:3000"
    environment:
      SCIM_BEARER_TOKEN: "replace-with-a-long-random-secret"
      DB_PATH: "/data/scim-demo.db"
    volumes:
      - scim_data:/data
volumes:
  scim_data:
